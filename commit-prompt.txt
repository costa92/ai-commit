只能输出以下格式，严禁任何其他内容：

<type>(<scope>): <subject>

要求：
1. 第一行必须是上述格式
2. subject必须是中文，不超过50字
3. 禁止输出英文、列表、解释、markdown
4. 禁止输出"The changes"、"1."、"*"等
5. 多文件修改时用高度概括的中文描述

示例：
feat(core): 重构核心模块
fix(api): 修复接口错误
refactor(all): 优化代码结构

以下是 git diff：
{{git_diff}}
